version: '2.4'
services:
  database:
    image: postgres
    container_name: products
    environment:
      POSTGRES_PASSWORD: banco123
      POSTGRES_DB: products
    ports:
      - "5434:5432"
    volumes:
      - devkaua_data:/var/lib/postgresql
volumes:
  devkaua_data: